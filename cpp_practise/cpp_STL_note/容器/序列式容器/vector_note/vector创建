1. 定义:
	vector<类型> 向量名;
2. 初始化:
	1. vector<类型> 向量名{初始化表};
	2. vector<类型> 向量名(长度);
	3. vector<类型> 向量名(长度,初始化值);
	4. vector<类型> 向量名(头指针,尾指针);
	5. vector<类型> 向量名(头迭代器,尾迭代器);
	6. vector<类型> 向量名(另一个vector);
	7. vector1.reserve(长度)
