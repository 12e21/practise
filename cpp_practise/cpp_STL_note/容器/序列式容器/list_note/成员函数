1. vector,deque成员函数基本都具有
2. 特有:
	1. splice():  	 将一个 list 容器中的元素插入到另一个容器的指定位置
	2. remove(val):  删除容器中所有等于 val 的元素
	3. unique():     删除容器中相邻的重复元素，只保留一个
	4. merge():      合并两个list容器
	5. sort():       将容器中元素排序
	6. reverse():    反转容器中元素顺序
